import React from "react";

interface Props {
    name?: string

}

const Sidebar: React.FC<Props> = ({}) => {
    return (
        <>
            this is sidebar.
        </>
    );
};

export default Sidebar;
